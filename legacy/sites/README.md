# Historical React/Worker release

The root application is now Python/Streamlit. This directory preserves the prior Sites-compatible React/Worker sources, frozen retrieval index, package lock, tests and evaluation reports. The existing hosted release remains unchanged.

For the complete original checkout—including its ingestion inputs and exact root layout—use Git commit `2f38162a4153539c31112c0f5eb6b430398d61db`. Do not run legacy ingestion commands against the new root Python corpus. Original narrative documentation is retained at `../../docs/legacy-web-readme.md` and `../../docs/legacy-web-project-report.md`; old relative links describe the historical checkout.

Do not deploy this directory to imply the Python migration is hosted: it is a distinct historical implementation with an earlier corpus and one-hop retrieval behavior.
